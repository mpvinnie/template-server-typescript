describe('CreateTodo', () => {
  it('2 + 2 should be 4', () => {
    const soma = 2 + 2
    const result = 4

    expect(soma).toBe(result)
  })

  it('2 + 2 should not be 5', () => {
    const soma = 2 + 2
    const result = 5

    expect(soma).not.toBe(result)
  })
})
